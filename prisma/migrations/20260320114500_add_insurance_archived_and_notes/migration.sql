-- AlterTable
ALTER TABLE `insurance` ADD COLUMN `notes` TEXT NULL,
    MODIFY COLUMN `status` ENUM('PENDING_APPROVAL', 'ACTIVE', 'EXPIRING_SOON', 'EXPIRING', 'EXPIRED', 'REJECTED', 'ARCHIVED') NOT NULL DEFAULT 'PENDING_APPROVAL';
